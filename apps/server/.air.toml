root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/server ./cmd/server"
entrypoint = "./tmp/server"
include_ext = ["go", "mod", "sum"]
exclude_dir = ["tmp", "dist", ".git", "vendor"]
delay = 1000
